<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Dashboard Estudiante - Trackademic' %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="/css/style.css" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #2563eb;
            --secondary-color: #64748b;
            --success-color: #16a34a;
            --warning-color: #ea580c;
            --danger-color: #dc2626;
            --light-bg: #f8fafc;
            --card-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
        }

        body {
            background-color: var(--light-bg);
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
        }

        .navbar-custom {
            background: linear-gradient(135deg, var(--primary-color) 0%, #1d4ed8 100%);
            box-shadow: var(--card-shadow);
            border: none;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: white !important;
        }

        .navbar-nav .nav-link {
            color: rgba(255, 255, 255, 0.9) !important;
            font-weight: 500;
            transition: color 0.2s ease;
        }

        .navbar-nav .nav-link:hover {
            color: white !important;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 0.375rem;
        }

        .search-hero {
            background: linear-gradient(135deg, var(--primary-color) 0%, #1d4ed8 100%);
            padding: 3rem 0;
            margin-bottom: 2rem;
        }

        .search-container {
            max-width: 800px;
            margin: 0 auto;
        }

        .search-input-group {
            position: relative;
            box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
        }

        .search-input {
            border: none;
            padding: 1rem 1rem 1rem 3rem;
            font-size: 1.1rem;
            border-radius: 0.75rem;
            box-shadow: none;
        }

        .search-input:focus {
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
            border-color: var(--primary-color);
        }

        .search-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--secondary-color);
            z-index: 10;
        }

        .search-btn {
            border-radius: 0.75rem;
            padding: 1rem 2rem;
            font-weight: 600;
            border: none;
            background: var(--success-color);
            transition: all 0.2s ease;
        }

        .search-btn:hover {
            background: #15803d;
            transform: translateY(-1px);
        }

        .card-custom {
            border: none;
            border-radius: 1rem;
            box-shadow: var(--card-shadow);
            transition: all 0.2s ease;
            background: white;
        }

        .card-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
        }

        .card-header-custom {
            background: transparent;
            border-bottom: 1px solid #e2e8f0;
            padding: 1.5rem;
            border-radius: 1rem 1rem 0 0 !important;
        }

        .card-title-custom {
            font-weight: 700;
            color: var(--primary-color);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-custom-primary {
            background: var(--primary-color);
            border: none;
            border-radius: 0.5rem;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .btn-custom-primary:hover {
            background: #1d4ed8;
            transform: translateY(-1px);
        }

        .btn-outline-custom {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            background: transparent;
            border-radius: 0.5rem;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .btn-outline-custom:hover {
            background: var(--primary-color);
            color: white;
        }

        .stats-card {
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
            border-left: 4px solid var(--primary-color);
        }

        .stats-number {
            font-size: 2rem;
            font-weight: 800;
            color: var(--primary-color);
        }

        .stats-label {
            color: var(--secondary-color);
            font-weight: 500;
            font-size: 0.9rem;
        }

        .plan-card {
            background: white;
            border-radius: 0.75rem;
            padding: 1.5rem;
            box-shadow: var(--card-shadow);
            transition: all 0.2s ease;
            border-left: 4px solid var(--primary-color);
        }

        .plan-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1);
        }

        .plan-subject {
            font-weight: 700;
            color: var(--primary-color);
            font-size: 1.1rem;
        }

        .plan-meta {
            color: var(--secondary-color);
            font-size: 0.9rem;
        }

        .badge-custom {
            padding: 0.5rem 1rem;
            border-radius: 9999px;
            font-weight: 600;
            font-size: 0.8rem;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 1rem;
            color: var(--secondary-color);
        }

        .empty-state i {
            font-size: 4rem;
            color: #cbd5e1;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <!-- Header/Navbar Unificado -->
    <%- include('../partials/navbar') %>

    <!-- Hero Section con Buscador Simplificado -->
    <section class="search-hero">
        <div class="container">
            <div class="search-container">
                <div class="text-center mb-4">
                    <h1 class="text-white fw-bold mb-3">¡Bienvenido de vuelta, <%= user.firstName || 'Estudiante' %>!</h1>
                    <p class="text-white opacity-75 fs-5">Gestiona tus notas y explora planes de evaluación</p>
                                </div>
                            </div>
                        </div>
    </section>

    <div class="container">
        <!-- Estadísticas rápidas -->
        <div class="row g-4 mb-5">
            <div class="col-md-3">
                <div class="card card-custom stats-card">
                    <div class="card-body text-center">
                        <div class="stats-number"><%= totalSubjects || 0 %></div>
                        <div class="stats-label">Materias Total</div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card card-custom stats-card">
                        <div class="card-body text-center">
                        <div class="stats-number"><%= averageGrade || '0.0' %></div>
                        <div class="stats-label">Promedio General</div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card card-custom stats-card">
                        <div class="card-body text-center">
                        <div class="stats-number"><%= passedSubjects || 0 %></div>
                        <div class="stats-label">Materias Aprobadas</div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card card-custom stats-card">
                        <div class="card-body text-center">
                        <div class="stats-number"><%= pendingActivities || 0 %></div>
                        <div class="stats-label">Actividades Pendientes</div>
                    </div>
                </div>
                    </div>
                </div>
                
        <!-- Sección Principal -->
        <div class="row g-4">
            <!-- Mis Planes Guardados -->
            <div class="col-lg-8">
                <div class="card card-custom">
                    <div class="card-header card-header-custom">
                        <h3 class="card-title-custom">
                            <i class="bi bi-bookmark-star-fill"></i>
                            Mis Planes de Evaluación
                        </h3>
                    </div>
                    <div class="card-body" id="myPlansContent">
                        <div class="empty-state" id="emptyState">
                            <i class="bi bi-bookmark"></i>
                            <h5>No tienes planes guardados aún</h5>
                            <p>Busca y guarda planes de evaluación para acceder rápidamente a ellos.</p>
                            <button class="btn btn-custom-primary" onclick="document.getElementById('searchInput').focus()">
                                <i class="bi bi-search me-2"></i>
                                Buscar Planes
                            </button>
                            </div>
                        <div id="plansContainer" style="display: none;">
                            <!-- Los planes se cargarán aquí dinámicamente -->
                        </div>
                    </div>
                </div>
            </div>

            <!-- Acciones Rápidas -->
            <div class="col-lg-4">
                <div class="card card-custom">
                    <div class="card-header card-header-custom">
                        <h3 class="card-title-custom">
                            <i class="bi bi-lightning-charge-fill"></i>
                            Acciones Rápidas
                        </h3>
                        </div>
                        <div class="card-body">
                        <div class="d-grid gap-3">
                            <a href="/courses/search" class="btn btn-custom-primary">
                                <i class="bi bi-search me-2"></i>
                                Explorar Cursos
                            </a>
                            <a href="/grades" class="btn btn-outline-custom">
                                <i class="bi bi-graph-up me-2"></i>
                                Ver Mis Notas
                            </a>
                            <a href="/reports" class="btn btn-outline-custom">
                                <i class="bi bi-file-text me-2"></i>
                                Reportes
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/app.js"></script>
    
    <script>
        // Función para mostrar los planes del estudiante
        async function showMyPlans() {
            const content = document.getElementById('myPlansContent');
            
            try {
                content.innerHTML = `
                    <div class="text-center py-4">
                        <div class="spinner-border text-primary" role="status">
                            <span class="visually-hidden">Cargando...</span>
                        </div>
                        <p class="mt-2">Cargando tus planes...</p>
                                                </div>
                `;

                const response = await fetch('/api/my-plans');
                const data = await response.json();

                if (data.success && data.plans.length > 0) {
                    content.innerHTML = data.plans.map(plan => `
                        <div class="plan-card mb-3">
                            <div class="d-flex justify-content-between align-items-start">
                                <div class="flex-grow-1">
                                    <div class="plan-subject">${plan.subjectCode}</div>
                                    <div class="plan-meta">
                                        ${plan.semester} - Grupo ${plan.groupNumber}
                                        <br>
                                        <small>Guardado: ${new Date(plan.savedAt).toLocaleDateString('es-ES')}</small>
                                    </div>
                                </div>
                                <div class="d-flex gap-2">
                                    <a href="/evaluation-plans/${plan._id}" class="btn btn-sm btn-primary">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a href="/evaluation-plans/${plan._id}/calculator" class="btn btn-sm btn-success">
                                        <i class="bi bi-calculator"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    `).join('');
                } else {
                    content.innerHTML = `
                        <div class="empty-state">
                            <i class="bi bi-bookmark"></i>
                            <h5>No tienes planes guardados aún</h5>
                            <p>Busca y guarda planes de evaluación para acceder rápidamente a ellos.</p>
                            <button class="btn btn-custom-primary" onclick="document.getElementById('searchInput').focus()">
                                <i class="bi bi-search me-2"></i>
                                Buscar Planes
                            </button>
                        </div>
                    `;
                }
            } catch (error) {
                console.error('Error loading plans:', error);
                content.innerHTML = `
                    <div class="text-center text-danger py-4">
                        <i class="bi bi-exclamation-triangle fs-2"></i>
                        <p>Error al cargar los planes</p>
                        <button class="btn btn-outline-primary" onclick="showMyPlans()">Reintentar</button>
                    </div>
                `;
            }
        }

        // Cargar planes al inicio
        document.addEventListener('DOMContentLoaded', function() {
            // Auto-focus en el buscador
            const searchInput = document.getElementById('searchInput');
            if (searchInput) {
                searchInput.addEventListener('input', function() {
                    // Agregar funcionalidad de autocompletado si es necesario
                });
            }
        });

        // Función para mostrar notificaciones
        function showToast(message, type = 'info') {
            // Implementar sistema de notificaciones
            const toast = document.createElement('div');
            toast.className = `alert alert-${type} position-fixed top-0 end-0 m-3`;
            toast.style.zIndex = '9999';
            toast.innerHTML = message;
            document.body.appendChild(toast);
            
            setTimeout(() => {
                toast.remove();
            }, 3000);
        }

        // Cargar planes guardados automáticamente al cargar el dashboard
        async function loadMyPlansForDashboard() {
            try {
                const response = await fetch('/api/my-plans');
                const data = await response.json();
                
                const emptyState = document.getElementById('emptyState');
                const plansContainer = document.getElementById('plansContainer');
                
                if (data.success && data.plans.length > 0) {
                    // Ocultar estado vacío y mostrar planes
                    emptyState.style.display = 'none';
                    plansContainer.style.display = 'block';
                    
                    plansContainer.innerHTML = `
                        <div class="row g-3">
                            ${data.plans.slice(0, 4).map(plan => `
                                <div class="col-md-6">
                                    <div class="card h-100 border-0 shadow-sm">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start mb-2">
                                                <h6 class="card-title text-primary mb-0">${plan.subjectCode}</h6>
                                                <span class="badge bg-success">Guardado</span>
                                            </div>
                                            <p class="card-text small text-muted">
                                                ${plan.semester} - Grupo ${plan.groupNumber}
                                                <br>
                                                <small>Nota actual: <strong>${plan.currentGrade.toFixed(1)}</strong></small>
                                            </p>
                                            <div class="d-flex gap-1">
                                                <a href="/evaluation-plans/${plan._id}" class="btn btn-sm btn-outline-primary">
                                                    <i class="bi bi-eye"></i> Ver
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            `).join('')}
                        </div>
                        ${data.plans.length > 4 ? `
                            <div class="text-center mt-3">
                                <button class="btn btn-outline-primary" onclick="showMyPlansModal()">
                                    Ver todos los planes (${data.plans.length})
                                </button>
                            </div>
                        ` : ''}
                    `;
                }
            } catch (error) {
                console.error('Error loading plans for dashboard:', error);
            }
        }

        // Cargar planes al inicializar la página
        document.addEventListener('DOMContentLoaded', loadMyPlansForDashboard);
        </script>
</body>
</html> 