<!DOCTYPE html>
<html lang="es" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Dashboard Estudiante - Trackademic' %></title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Toastify CSS -->
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
    <!-- Custom CSS -->
    <link href="/css/style.css" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #2563eb;
            --secondary-color: #64748b;
            --success-color: #16a34a;
            --warning-color: #ea580c;
            --danger-color: #dc2626;
            --light-bg: #f8fafc;
            --card-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
        }

        body {
            background-color: var(--light-bg);
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
        }

        .navbar-custom {
            background: linear-gradient(135deg, var(--primary-color) 0%, #1d4ed8 100%);
            box-shadow: var(--card-shadow);
            border: none;
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: white !important;
        }

        .navbar-nav .nav-link {
            color: rgba(255, 255, 255, 0.9) !important;
            font-weight: 500;
            transition: color 0.2s ease;
        }

        .navbar-nav .nav-link:hover {
            color: white !important;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 0.375rem;
        }

        .search-hero {
            background: linear-gradient(135deg, var(--primary-color) 0%, #1d4ed8 100%);
            padding: 3rem 0;
            margin-bottom: 2rem;
        }

        .search-container {
            max-width: 800px;
            margin: 0 auto;
        }

        .search-input-group {
            position: relative;
            box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
        }

        .search-input {
            border: none;
            padding: 1rem 1rem 1rem 3rem;
            font-size: 1.1rem;
            border-radius: 0.75rem;
            box-shadow: none;
        }

        .search-input:focus {
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
            border-color: var(--primary-color);
        }

        .search-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--secondary-color);
            z-index: 10;
        }

        .search-btn {
            border-radius: 0.75rem;
            padding: 1rem 2rem;
            font-weight: 600;
            border: none;
            background: var(--success-color);
            transition: all 0.2s ease;
        }

        .search-btn:hover {
            background: #15803d;
            transform: translateY(-1px);
        }

        .card-custom {
            border: none;
            border-radius: 1rem;
            box-shadow: var(--card-shadow);
            transition: all 0.2s ease;
            background: white;
        }

        .card-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
        }

        .card-header-custom {
            background: transparent;
            border-bottom: 1px solid #e2e8f0;
            padding: 1.5rem;
            border-radius: 1rem 1rem 0 0 !important;
        }

        .card-title-custom {
            font-weight: 700;
            color: var(--primary-color);
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-custom-primary {
            background: var(--primary-color);
            border: none;
            border-radius: 0.5rem;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .btn-custom-primary:hover {
            background: #1d4ed8;
            transform: translateY(-1px);
        }

        .btn-outline-custom {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            background: transparent;
            border-radius: 0.5rem;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .btn-outline-custom:hover {
            background: var(--primary-color);
            color: white;
        }

        .stats-card {
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
            border-left: 4px solid var(--primary-color);
        }

        .stats-number {
            font-size: 2rem;
            font-weight: 800;
            color: var(--primary-color);
        }

        .stats-label {
            color: var(--secondary-color);
            font-weight: 500;
            font-size: 0.9rem;
        }

        .plan-card {
            background: white;
            border-radius: 0.75rem;
            padding: 1.5rem;
            box-shadow: var(--card-shadow);
            transition: all 0.2s ease;
            border-left: 4px solid var(--primary-color);
        }

        .plan-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1);
        }

        .plan-subject {
            font-weight: 700;
            color: var(--primary-color);
            font-size: 1.1rem;
        }

        .plan-meta {
            color: var(--secondary-color);
            font-size: 0.9rem;
        }

        .badge-custom {
            padding: 0.5rem 1rem;
            border-radius: 9999px;
            font-weight: 600;
            font-size: 0.8rem;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 1rem;
            color: var(--secondary-color);
        }

        .empty-state i {
            font-size: 4rem;
            color: #cbd5e1;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <!-- Header/Navbar Unificado -->
    <%- include('../partials/navbar') %>

    <!-- Hero Section con Buscador Simplificado -->
    <section class="search-hero">
        <div class="container">
            <div class="search-container">
                <div class="text-center mb-4">
                    <h1 class="text-white fw-bold mb-3">¡Bienvenido de vuelta, <%= user.firstName || 'Estudiante' %>!</h1>
                    <p class="text-white opacity-75 fs-5">Gestiona tus notas y explora planes de evaluación</p>
                </div>
            </div>
        </div>
    </section>

    <div class="container">
        <!-- Estadísticas rápidas -->
        <div class="row g-4 mb-5">
            <div class="col-md-3">
                <div class="card card-custom stats-card">
                    <div class="card-body text-center">
                        <div class="stats-number"><%= totalSubjects || 0 %></div>
                        <div class="stats-label">Materias Total</div>
                        <% if ((totalSubjects || 0) === 0) { %>
                            <small class="text-muted">Guarda tu primer plan</small>
                        <% } %>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card card-custom stats-card">
                    <div class="card-body text-center">
                        <div class="stats-number"><%= typeof averageGrade === 'number' ? averageGrade.toFixed(2) : '0.0' %></div>
                        <div class="stats-label">Promedio General</div>
                        <% if ((totalSubjects || 0) === 0) { %>
                            <small class="text-muted">Ingresa notas</small>
                        <% } %>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card card-custom stats-card">
                    <div class="card-body text-center">
                        <div class="stats-number"><%= passedSubjects || 0 %></div>
                        <div class="stats-label">Materias Aprobadas</div>
                        <% if ((passedSubjects || 0) === 0 && (totalSubjects || 0) > 0) { %>
                            <small class="text-muted">Completa actividades</small>
                        <% } %>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card card-custom stats-card">
                    <div class="card-body text-center">
                        <div class="stats-number"><%= pendingActivities || 0 %></div>
                        <div class="stats-label">Actividades Pendientes</div>
                        <% if ((pendingActivities || 0) === 0 && (totalSubjects || 0) > 0) { %>
                            <small class="text-success">¡Todo al día!</small>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
                
        <!-- Sección Principal -->
        <div class="row g-4">
            <!-- Mis Planes Guardados -->
            <div class="col-lg-8">
                <div class="card card-custom" id="mis-planes">
                    <div class="card-header card-header-custom">
                        <h3 class="card-title-custom">
                            <i class="bi bi-bookmark-star-fill"></i>
                            Mis Planes de Evaluación
                        </h3>
                    </div>
                    <div class="card-body" id="myPlansContent">
                        <div class="empty-state" id="emptyState">
                            <i class="bi bi-bookmark"></i>
                            <h5>No tienes planes guardados aún</h5>
                            <p>Busca y guarda planes de evaluación para acceder rápidamente a ellos.</p>
                            <button class="btn btn-custom-primary" id="searchPlansBtn">
                                <i class="bi bi-search me-2"></i>
                                Buscar Planes
                            </button>
                        </div>
                        <div id="plansContainer" style="display: none;">
                            <!-- Los planes se cargarán dinámicamente aquí -->
                        </div>
                    </div>
                </div>
            </div>

            <!-- Acciones Rápidas -->
            <div class="col-lg-4">
                <div class="card card-custom">
                    <div class="card-header card-header-custom">
                        <h3 class="card-title-custom">
                            <i class="bi bi-lightning-charge-fill"></i>
                            Acciones Rápidas
                        </h3>
                        </div>
                        <div class="card-body">
                        <div class="d-grid gap-3">
                            <a href="/courses/search" class="btn btn-custom-primary">
                                <i class="bi bi-search me-2"></i>
                                Explorar Cursos
                            </a>
                            <a href="/grades" class="btn btn-outline-custom">
                                <i class="bi bi-graph-up me-2"></i>
                                Ver Mis Notas
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Toastify JS -->
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
    <!-- Custom JS -->
    <script src="/js/app.js"></script>
    <script src="/js/dashboard.js"></script>
    
    <script>
        // Manejar scroll automático cuando se accede con hash
        document.addEventListener('DOMContentLoaded', function() {
            // Verificar si hay un hash en la URL
            if (window.location.hash === '#mis-planes') {
                // Esperar un poco para que el contenido se cargue
                setTimeout(() => {
                    const element = document.getElementById('mis-planes');
                    if (element) {
                        element.scrollIntoView({ 
                            behavior: 'smooth', 
                            block: 'center' 
                        });
                        
                        // Agregar efecto visual temporal
                        element.style.transition = 'box-shadow 0.3s ease';
                        element.style.boxShadow = '0 0 20px rgba(37, 99, 235, 0.3)';
                        
                        setTimeout(() => {
                            element.style.boxShadow = '';
                        }, 2000);
                    }
                }, 500);
            }
        });
    </script>
</body>
</html> 